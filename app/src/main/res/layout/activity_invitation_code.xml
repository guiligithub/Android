<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/activity_horizontal_margin"
    tools:context=".ui.auth.InvitationCodeActivity">

    <EditText
        android:id="@+id/invitation_code_edit"
        style="@style/AppEdit"
        android:background="@drawable/white_gray_radius_bg"
        android:importantForAutofill="no"
        android:inputType="number"
        android:padding="@dimen/activity_horizontal_margin"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="LabelFor" />

    <TextView
        android:id="@+id/invitation_code_submit"
        style="@style/AppBtn"
        android:layout_marginTop="@dimen/common_btn_height"
        android:text="@string/save"
        app:layout_constraintTop_toBottomOf="@+id/invitation_code_edit"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>